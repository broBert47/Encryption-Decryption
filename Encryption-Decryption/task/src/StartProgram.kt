package encryptdecrypt
/*
class StartProgram(
    val mode: Mode = Mode.ENC,
    val key: EncryptionKey = EncryptionKey.defaultKey(),
    val data: Data = Data.noData()
) {
    fun execute(){

    }
}

 */